# Refactoring Summary: Network Reconstruction (Prompt 2)

We have refactored the script `cruise_step1_generate_edgelist_weighted.R` to reconstruct the networks according to the updated requirements in `2.md`.

## Changes Implemented
1. **Time-Aggregated Static Network**:
   - Aggregated contact events in `dataContact` where `SAIL == 1` over the 4 days of the cruise sailing.
   - For each unique pair (`nodeID_from` = `ID.x`, `nodeID_to` = `ID.y`), we calculate:
     - `total_contact_duration`: The sum of all interaction durations between the two nodes.
     - `number_of_contacts`: The count of interactions (contacts) between the two nodes.
   - Saved the output as a CSV file to `data/project_39/static_network_sail_1.csv`.

2. **Temporal Network**:
   - Extracted active interactions for `SAIL == 1` day-by-day.
   - Selected and renamed columns to:
     - `time` (from `DAY_INTERACT`)
     - `nodeID_from` (from `ID.x`)
     - `nodeID_to` (from `ID.y`)
     - `contact_duration` (from `DURATION`)
     - `location_or_setting_if_available` (from `CONTACT_TYPE`)
   - Saved the output as a CSV file to `data/project_39/temporal_network_sail_1.csv`.

3. **Wrong Path & Library Loading Correction**:
   - Sourced `cruise_load_library.R` from the local directory instead of `github/code/`.
   - Corrected the data loading path to load `dataNodes.RData` and `dataContact.RData` relatively from `../../data/project_39/`.
