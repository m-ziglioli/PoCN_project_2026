# Modified `simulate_for_different_beta` Function

We have successfully rewritten the `simulate_for_different_beta` function to run `n_runs` simulations for each beta value and calculate the average and standard deviation of the recovered fraction. It also utilizes `mclapply` from the `parallel` package to parallelize these runs.

The implementation has been saved to `make_runs.R`.
