fix make_runs: NA values are saved in csv. sabve in csv I/N and R/N with theri std


all scheduled cores encountered errors in user code
3: In mclapply(1:n_runs, function(run) { :
all scheduled cores encountered errors in user c rebuild the whole make_runs file and check consistencu with epidemic_model.R mcapply, 30 runs, save fraction of I and R